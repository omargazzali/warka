import {StyleSheet, Dimensions} from 'react-native';

export default StyleSheet.create({
    view: {
        width: '100%',
        height: '100%',
        alignItems: 'center',
        backgroundColor:'#fff'
    },
    imageBack: {
        width: '100%',
        height: 511,
    },
    imageSign: {
        width: 180,
        height: 90,
        alignItems: 'center',
        justifyContent: 'center',
        marginBottom:'10%'
    }
});